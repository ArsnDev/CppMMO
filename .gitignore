
# -------------------------------------------------------------
# 1. Build Output Directories & Generated Files
# -------------------------------------------------------------
# Visual Studio's primary CMake output directory
out/
# Traditional build directory (if you create it manually or use it for specific builds)
build/

# CMake generated files (often within out/ or build/, but good to explicitly ignore)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
# CMakePresets.json # Keep this line commented if you intend to commit CMakePresets.json
# CMakeUserPresets.json # Typically user-specific, so usually ignored

# -------------------------------------------------------------
# 2. Visual Studio Specific Files & Folders (CRITICAL for large files)
# -------------------------------------------------------------
# The hidden .vs/ folder, contains user-specific settings, IntelliSense databases, etc.
# These often grow very large and should NEVER be committed.
.vs/

# Visual Studio traditional project files (generated by VS for CMake integration)
# These files should also be ignored as they are derived from CMakeLists.txt.
*.sln               # The main solution file
*.vcxproj           # C++ project files
*.vcxproj.filters   # Filters for solution explorer organization
*.vcxproj.user      # Per-user project settings

# Other common VS temporary/metadata files
*.vspscc            # Visual Studio Source Control SCC files
*.suo               # Solution User Options (old format)
*.opendb
*.tlog              # Incremental build logs
*.VC.db             # Visual Studio Database (large IntelliSense/code analysis DB)
*.sdf               # SQL Server Compact Database file (older IntelliSense)
*.ipch              # Precompiled header info cache (can be very large)
*.log               # Various build-related log files written by VS

# -------------------------------------------------------------
# 3. Intermediate Build Products & Debugging Files
# -------------------------------------------------------------
*.obj               # Object files
*.pch               # Precompiled header files
*.ilk               # Incremental linker files
*.pdb               # Program database (debugging info)
*.tmp               # Temporary files generated during compilation/linking
*.exe               # Executable files
*.dll               # Dynamic Link Libraries
*.lib               # Library files (static or import)
*.so                # Shared objects (Linux)
*.a                 # Static archives (Linux)
*.dylib             # Dynamic libraries (macOS)
*.dmp               # Dump files (crash reports)
*.stackdump         # Stack dump files

# -------------------------------------------------------------
# 4. External Libraries & Dependencies (managed by FetchContent or placed manually)
# -------------------------------------------------------------
# This ignores the entire 'external/' directory.
# This is crucial as it contains Boost (potentially large) and other library sources.
# FetchContent downloads also typically go into out/build/_deps, which is already covered by 'out/'.
external/

# -------------------------------------------------------------
# 5. FlatBuffers Generated Files
# -------------------------------------------------------------
# These files are generated from FlatBuffers schema (.fbs) and should not be version controlled.
# Adjust patterns if your generated files have different naming conventions or locations.
*_generated.h
*_generated.cpp
*.fbs.h # Common pattern for auto-generated FlatBuffers headers

# -------------------------------------------------------------
# 6. Operating System Specific & Editor Files
# -------------------------------------------------------------
*.swp               # Vim swap files
*~                  # Emacs backup files
.#* # Emacs lock files
.DS_Store           # macOS specific files
Thumbs.db           # Windows thumbnail cache

# -------------------------------------------------------------
# 7. Application-Specific Log Files
# -------------------------------------------------------------
logs/

# -------------------------------------------------------------
# 8. Doxygen Output Files
# -------------------------------------------------------------
docs/